{
  "name": "poof-sounds",
  "version": "4.0.0-0",
  "description": "build scripts for poof sounds",
  "scripts": {
    "build": "npm run validate && node build_scripts/build.js",
    "build:dev": "npm run validate:dev && node build_scripts/build.js dev",
    "preversion": "npm run validate",
    "validate": "node build_scripts/validate.js",
    "validate:dev": "node build_scripts/validate.js dev"
  },
  "dependencies": {
    "adm-zip": "^0.5.10",
    "ffprobe": "^1.1.2",
    "ffprobe-static": "^3.1.0",
    "jimp": "^0.22.10",
    "loglevel": "^1.8.1",
    "semver": "^7.5.4"
  },
  "devDependencies": {
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^8.8.0",
    "prettier": "^2.8.8"
  }
}
