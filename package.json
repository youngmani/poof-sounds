{
  "name": "poof-sounds",
  "version": "4.0.0",
  "description": "build scripts for poof sounds",
  "scripts": {
    "build": "node build_scripts/build.js",
    "build:beta": "node build_scripts/build.js beta",
    "preversion": "npm run validate",
    "validate": "node build_scripts/validate.js"
  },
  "dependencies": {
    "adm-zip": "^0.5.10",
    "ffprobe": "^1.1.2",
    "ffprobe-static": "^3.1.0",
    "jimp": "^0.22.10",
    "semver": "^7.5.4",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^8.8.0",
    "prettier": "^2.8.8"
  }
}
